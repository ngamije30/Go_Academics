
# Dashboard

Placeholder for the teacher-facing dashboard (React or Streamlit — see
CLAUDE.md for context). Not yet scaffolded with actual framework files;
decide React vs. Streamlit before running `npx create-react-app` or
`pip install streamlit` here.

Planned screens:

- Login / school selector
- Student risk list (sortable by risk level)
- Individual student risk detail (SHAP explanation, phase history)
- Phase overview (which phase the term is currently in)
