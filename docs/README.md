# Docs

Place capstone documentation artifacts here:
- `research_proposal.docx`
- `data_collection_instrument.docx`
- Diagram exports (system architecture, use case, ERD, class, sequence, ML pipeline)
- Ethics form

Keeping these alongside the code (rather than only in Google Drive) makes
it easy for Claude Code to reference exact wording/decisions already
agreed on, instead of re-deriving them.
